To install the package:

    python setup.py sdist
    sudo easy_install build/copilot_dashboard...

Installing the package will automatically install required dependencies. (except for gunicorn which should have been installed by Co-Pilot Monitor)

To run the Dashboard just type:

    copilot-dashboard

or to see available options:

    copilot-dashboard --help
